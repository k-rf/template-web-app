{
  "name": "template_web_app",
  "version": "0.0.0",
  "description": "The web application template.",
  "private": true,
  "scripts": {
    "prepare": "husky install"
  },
  "lint-staged": {
    "*.{json,md}": [
      "prettier --write"
    ]
  },
  "devDependencies": {
    "husky": "^7.0.4",
    "lint-staged": "^12.4.1",
    "prettier": "^2.6.2"
  }
}
